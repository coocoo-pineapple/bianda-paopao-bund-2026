# 变大泡泡 · 小游戏平台 交接文档

> 给「无记忆新窗口」用。读完这份就能开工，不用翻历史对话。
> 工作目录 `D:\bianda-paopao`，起服务 `PORT=3457 node server/index.js`，入口 `http://localhost:3457/app.html`。

---

## 一、产品前提（一句话）

**变大泡泡 = 伪装成一台假 Windows 桌面的匿名跨公司职场社区。**
所有真功能都套在正经办公软件的壳子里：社区广场是 Word、数据是 Excel、看板是 BI、聊天是钉钉。
「趣味游戏」文件夹里那一堆小游戏，对内是玩法，对外是**玩家自制内容平台 + 打赏变现**的证据——这是给评委看的商业闭环，不是彩蛋。

技术栈：**零构建**。express 静态服务 + 三个前端文件，没有打包器、没有框架。
- `public/app.html` —— 全部 CSS + 全部标记 + 一部分内联 JS（约 3700 行）
- `public/os.js` —— 假 Windows 桌面层（窗口工厂 / 文件夹 / 任务栏 / 各增强模块）
- `public/shells.js` —— 各"办公软件外壳"的内容渲染（BI 看板、点子集市、皮肤商城…）
- `public/games/*.js` —— 独立小游戏，每个一个文件，`<script defer>` 挂在 app.html 末尾

---

## 二、现有小游戏 · 概念总表

一共 8 个条目在「趣味游戏」文件夹里（`os.js` 的 `GAME_ITEMS` 数组）。
**共同设计法则：每个游戏都得是真能玩的游戏，只是穿了办公软件的皮。** 不做"点一下出一句话"的假交互。

| 名字 | 办公壳 | 玩法内核 | 职场笑点 | 代码位置 |
|---|---|---|---|---|
| **拼豆** | Excel 单元格底色测试 | 30×18 网格涂色画像素画，10 色调色板，按住拖动连续填 | 「格子是真的，鱼也是真的」——上班摸鱼画画，老板路过看到的是 Excel | `app.html` 内联（`buildSheet` 附近）+ `os.js` 的 `xlRealism()` |
| **梦蝶局** | PowerPoint 放映 | 桌游发牌局：翻牌入场 / 谁说话谁出牌 / 押注点牌 / 揭底盖戳，猜四个 AI 里谁是假猎头 | 庄周梦蝶——分身、马甲、换个形态就挣脱一处困境 | `app.html`（`#dreamGame`，1624 行起） |
| **爽文背单词** | 独立窗口（深色鎏金） | 24 条职场黑话，每题 1 正解 + 2 干扰项，8 词一局，连击加成，10 秒倒计时，五段境界（职场小白→会话嘴替→黑话十级→装逼宗师→庄周） | 「抓手」的正解是"我也不知道从哪开始，先说个词稳住场面"；「闭环」是"事情没做成，但汇报做完了" | `games/word.js` |
| **捡手机文学** | 手机投屏窗口 | 会议室捡到一部手机，一条一条往下翻聊天记录，微信风气泡，多个故事可切换 | 别人的人生，看到一半发现自己是那个背锅的 | `app.html` 的 `renderPhone()` + `PHONE_STORIES` |
| **职场五子棋** | 会议室占用 | 真五子棋 13×13，老板执黑先手占天元，启发式 AI（五连 1e6 / 活四 1e5 / 冲四 1e4 / 活三 5e3…，攻守权重 `max(attack*1.1, defense)`），本地存战绩 | 「黑=老板·白=你·老板先手」——你永远后手 | `games/gomoku.js` |
| **工位躲猫猫** | 工位平面图 | 4×5=20 个工位翻牌找人，**每次扑空目标会往相邻工位溜一格**（包括你查过的），曼哈顿距离温度提示，三档难度 8/6/4 步 | 「他躲过了你，就像躲过了周报」 | `games/hide.js` |
| **皮肤商城** | 独立窗口 | 玩家做的水面滤镜皮肤，试穿立刻改整个桌面背景（`#stage` 的 CSS filter），缩略图是桌面的迷你版所见即所得 | 「换一片水，不换鱼」——皮肤跟马甲走不跟人走 | `shells.js` 的 `SKINS` / `buildSkinShop()` |
| **放映室** | 电视窗口 | 贴个视频链接就播 | 摸鱼的尽头 | `app.html` 的 `tvWin` |

另有 **点子集市**（在「技能集市」文件夹里）= 游戏商店本身：列出四个游戏，每个带「去玩」+「赏 ¥1」，文案写死了分成规则「九成归他，一成归平台」。
数据在 `shells.js` 的 `GAMES` 数组 / `buildGameHall()`。

---

## 三、要新增的东西（本次工单）

### 3.1 职场 MBTI 测试（官方游戏，新增第 9 个条目）

不是抄真 MBTI，是**职场版恶搞四维**。建议维度（可自己重拟，保住讽刺感）：
- **会不会说话**：黑话流利 ↔ 有一说一
- **加不加班**：主动卷 ↔ 准点消失
- **背不背锅**：主动接锅 ↔ 甩得干净
- **信不信画饼**：真信 ↔ 听听就好

产出：16 型人格，每型一个职场外号 + 一句刻薄评语 + 「适合的工位」+ 「天敌型号」。
壳子建议套 **Outlook 邮件里的"人才盘点问卷"** 或 **Word 的"新员工性格测评表"**——继续伪装，别做成一个明晃晃的测试页。
结果页要能「转成泡泡」发到广场（现有 `mk()` 接口），这样测试也进转化漏斗。

### 3.2 用户自己上传自制小游戏

这是本次的重点，也是给评委看的**平台叙事**：官方带头做几个 → 玩家自己传 → 别人玩了打赏 → 九成归作者。
点子集市的文案已经写好了这套规则，现在要把「上传」这条路真做出来。

要考虑清楚的几件事（新窗口自己定方案，不要照抄）：
- 上传什么格式？最省事是单个 HTML/JS 片段，跑在沙箱 iframe 里
- 沙箱怎么隔离（`sandbox` 属性、不给同源、限制尺寸）
- 上传后落在哪：内存态就够 hackathon 演示，还是要落盘/后端
- 上架流程要不要审核态（待审/已上架），这个能给评委讲"平台治理"
- 上架后必须自动出现在**点子集市列表**和**趣味游戏文件夹**里，走现有的 `GAMES` 数组 + `GAME_ITEMS` 数组
- 没有生成图标的自制游戏，走现成的兜底：文件夹图标会自动退回"首字色块"（见 `mkFolder` 的探测逻辑）

---

## 四、接入点速查（改这几处就能加一个游戏）

```js
// 1) os.js —— 创建窗口。返回的是窗体 body，往里塞内容就行
const body = mkWin('winMbti', 'word', '新员工性格测评表.docx - Word', 'left:400px;top:100px;width:460px;height:520px');

// 2) os.js —— 注册路由。goFeature('mbti') 就能开这扇窗
OS_GO.mbti = ['winMbti'];
// 带子视图的写法（Excel 那种一个窗口多张表）：OS_GO.draw = ['winExcel', 'v-draw'];

// 3) os.js 的 GAME_ITEMS 数组 —— 进「趣味游戏」文件夹
// 格式：[单字, 名字, 副标题, 主题色, 打开函数, 图标路径]
['测', '职场MBTI', '你是哪种牛马', '#7A5AA8', () => goFeature('mbti'), 'icons/it-mbti.png'],

// 4) shells.js 的 GAMES 数组 —— 进「点子集市」商店列表
{ nm: '职场MBTI', by: '官方', tip: 0, go: 'mbti', ic: 'it-mbti' },

// 5) 全局提示气泡
osToast('测完了 · 你是「会议室隐形人」');
```

**「搬家不重建」模式**：如果内容已经存在于 app.html 的某张卡片里，别重写一遍，用 `splitWins()` 里的 `adopt(选择器, 窗口id, 标题, 样式)` 把 DOM 整块搬进新窗口。

**独立游戏文件模式**（推荐给复杂游戏，见 `games/*.js`）：
```js
(function () {
  var win = document.querySelector('#winXXX'); if (!win) return;
  var b = win.querySelector('.dwb'); if (!b) return;
  b.innerHTML = '';                       // 必须：接管整个窗体，别叠在旧内容上
  var css = document.createElement('style');
  css.textContent = '#winXXX .xxx{...}';  // 样式全部锁在 #winXXX 里，别污染全局
  b.appendChild(css);
  // …游戏逻辑…
})();
```
然后在 `app.html` 末尾加 `<script src="games/xxx.js" defer></script>`。

**性能红线**（这是网页，用户会卡）：
- 游戏文件里 **不准用 `setInterval`**，倒计时用 CSS transition + 单个 `setTimeout`
- 事件用委托，一个容器一个监听器，不要每格绑一个
- 动画走 CSS，不走 JS 逐帧
- 粒子/碎屑用 `animationend` 自清理，再加一个 `setTimeout` 兜底

---

## 五、生图 · 完整操作手册

### 脚本与参数

```bash
powershell -File "C:\Users\admin\Documents\New project\generate_image_openai.ps1" \
  -Model "gpt-image-2-count" \
  -Prompt "提示词" \
  -Size 1024x1024 \
  -Quality high \
  -Background transparent \
  -OutFile "D:\bianda-paopao\public\assets\icons\it-mbti.png" \
  -Retries 2
```

**关键坑（必看）：**
- **`-Model` 必须写 `gpt-image-2-count`**。默认的 `gpt-image-2` 和 `gpt-image-1` 都会报 `503 model_not_found ... 无可用渠道`。不确定时先查 `/v1/models`。
- 服务地址 `https://ai.beehears.com/v1`（**不是** cf.beehears.com）
- key 在 `C:\Users\admin\Documents\New project\.env` 的 `BEEHEARS_API_KEY`（**不在** `~/.codex/.env`）
- 尺寸只支持 `1536x1024` / `1024x1024` / `1024x1536`
- 脚本**拒绝覆盖已存在的文件**——这刚好可以拿来做断点续跑
- 垫图走 `-ReferenceImage 参考图.png`（走 edits 端点）

### 批量生成的脚本骨架

直接抄 `tools/gen_item_icons.sh`，它已经处理好了断点续跑和失败退避：
- 一个 `TASKS` 数组，每行 `文件名|提示词`
- 外层 `for round in $(seq 1 16)`，文件已存在就 skip
- 生成失败 `sleep 120` 再重试下一轮
- 全部落地就 `exit 0`

同目录还有 `gen_icons.sh` / `gen_bots.sh` / `gen_fish.sh` / `gen_polish.sh` 可参考。

### 两套已验证的提示词风格

**A. 图标（透明底，Win11 风）** —— 用于所有 `icons/it-*.png`：
```
{具体物件描述}, Windows 11 Fluent Design style 3D app icon, soft gradients,
subtle depth, front-facing, centered composition, isolated on transparent
background, no text, no watermark, clean, high detail
```
配 `-Size 1024x1024 -Quality high -Background transparent`。

**B. 场景背景（深蓝纸雕 + 鎏金）** —— 用于窗口背景、封面，和产品封面同一个调性：
关键词组合是 `deep navy paper-craft / layered paper cut / gold foil accents / warm rim light / cinematic depth`，
横图用 `1536x1024`，竖图（手机壁纸）用 `1024x1536`。

### 生成后必做两件事

1. **压缩**：`powershell -File tools\shrink_assets.ps1`
   图标降到 256px、角色立绘 512px、场景背景 900px。
   不压的话图标目录能到 30MB，网页加载直接卡死；压完是 2.2MB。
   场景类照片图另存 JPEG（43–136KB，PNG 要 1–2MB），代码里引用记得写 `.jpg`。

2. **接探测加载**：图 404 时必须优雅退回，绝不能裂。两种现成写法——

```js
// 写法一：文件夹小图标（mkFolder 里已实现，加数组条目就自动生效）
const probe = new Image();
probe.onload = () => { tile.classList.add('pic'); tile.style.backgroundImage = `url(assets/${img})`; };
probe.src = 'assets/' + img;
// 图没生成 → 保持"首字色块"，界面照样完整

// 写法二：窗口背景（os.js 的 polishAssets）
const im = new Image();
im.onload = () => document.querySelector('#winXXX').classList.add('hasbg');
im.src = 'assets/xxx.jpg';
// CSS 里 #winXXX.hasbg .dwb{background:...url(...)} —— 没有 hasbg 就走纯 CSS 兜底
```

### 现有素材清单（别重复生成）

- `assets/icons/` 22 个：`it-draw/it-dream/it-word/it-phone/it-gomoku/it-hide/it-skin/it-skillmart/it-mail/it-hall/it-ops/it-salary/it-jobs/it-daily/it-pitch` + `fold-games/fold-mart/fold-info`（文件夹）+ `doc-word/doc-pdf/film/bin`
- 场景背景：`gomoku-wood.jpg`（木纹棋盘）/ `hide-office.jpg`（俯视工位）/ `wordgame-bg.jpg`（金光书卷）/ `phone-wall.jpg`（手机壁纸）/ `bi-bg.jpg`（看板）/ `dream-table.jpg`（梦蝶局牌桌）
- 吉祥物（庄周的鱼，纸雕风）：`fish-perch.png`（趴窗沿）/ `fish-farmer.png`（菜园）/ `fish-carry.png`（搬运）/ `bi-fish.png`
- 封面：`cover-fish2.webp`（就是桌面背景本体）/ `cover-well.webp`

**本次要新生成的**：`icons/it-mbti.png`，以及用户上传游戏的默认占位图标 `icons/it-custom.png`（建议画成"一个空白卡带 / 待上架的盒子"）。

---

## 六、验收

1. `node --check public/os.js && node --check public/shells.js` 过
2. 浏览器逐窗截图核对：MBTI 走完一遍能出结果并能转成泡泡；上传一个自制游戏后，点子集市和趣味游戏文件夹里都能看到并能打开
3. **兜底测试**：临时把新生成的图标改名，刷新确认界面不裂（退回首字色块），测完改回来
4. 生成图后跑 `tools\shrink_assets.ps1`，确认 `public/assets/icons` 不超过 3MB

---

## 七、给新窗口的开场白

```
在 D:\bianda-paopao 工作，先读 HANDOFF_游戏平台_2026-08-10.md，那里面有全部背景。

这是个伪装成假 Windows 桌面的职场社区，「趣味游戏」文件夹里已经有 8 个真能玩的小游戏。
这次要加两块：
1. 职场 MBTI 测试 —— 恶搞四维、16 型人格，套在办公软件壳子里（Outlook 问卷或 Word 测评表），
   结果能「转成泡泡」发到广场
2. 用户上传自制小游戏 —— 玩家能传自己写的小游戏，上架后自动出现在点子集市和趣味游戏文件夹，
   别人玩了能打赏。沙箱隔离方案你自己定

零构建栈，改 public/app.html + public/os.js + public/shells.js + public/games/*.js。
接入点、生图脚本参数、性能红线、兜底写法交接文档里都写了，照着来。

验收：node --check 过；MBTI 能走完出结果；上传的游戏能在两个入口打开；
把新图标改名刷新界面不裂。

读完先回我一句你打算怎么做，等我确认再开干。
```
