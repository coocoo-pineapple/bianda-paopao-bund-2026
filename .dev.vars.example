# 变大泡泡 · 本地 wrangler dev 用的 secret
# 复制为 .dev.vars（已在 .gitignore 里）。线上不读这个文件，
# 线上用：npx wrangler secret put AI_API_KEY
#
# 非机密配置（AI_STYLE / AI_BASE_URL / AI_MODEL / 限额）在 wrangler.jsonc 的 vars 里。

AI_API_KEY=
