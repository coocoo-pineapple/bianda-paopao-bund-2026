# PLAN · 冲刺重排（0810 · v2）

> **前置**：先读 `docs/NARRATIVE_v2.md`（叙事定稿，唯一事实源）。本计划的每一条都从它派生。
> 旧版计划（P0=面板/合规/竞品）已作废——那版还停在"补短板"，这版是**换叙事**。
>
> 核心判断：**视觉和功能都已经溢出，真正的瓶颈是"评委看完能不能复述出我们是什么"。**
> 所以这轮的重心从「补功能」移到「统一叙事 + 亮技术」。

---

## 0 · 公共约束（所有窗口先读）

- **`server/` 归 Codex**。AI 接入、key、`/api/ask` 字段扩展，全部不碰。key 不要问用户、不要打印、不要写进代码或文档
- **参赛仓 `bianda-paopao-bund-2026` 只有 Codex 能提交**，任何窗口不准往那边推
- **动手前先 `git status`**。当前未提交改动横跨 `public/os.js`、`public/app.html`、`public/shells.js`、`server/*`、素材压缩——**都不是本计划产生的**，是三个窗口在并行。改之前重读目标区域，别拿旧行号下手
- 改完 `npm start` 能跑，`node tools/build-dist.js` 能出单机版并过 `$$` 自检
- 用 Edit 工具改文件，不用 sed/awk/echo
- 不加计划外的功能、注释、错误处理；commit 信息带工单编号

---

## 1 · 工单一览（按重要性重排）

| 编号 | 名称 | 优先级 | 归属 | 预计 |
|---|---|---|---|---|
| **DM-02** | 梦蝶局：视野隔离 + 案例卷宗 | ~~P0~~ **已完成** | 梦蝶局窗口 | — |
| **W1** | AI 调用记录面板 | **P0（进行中）** | 窗口 A（已派） | 2h |
| **W2** | 产品 slides 重写（应用内 PPT + PDF + pptx **三份**）+ 措辞清洗 | **P0** | 窗口 C | 3h |
| **W3** | 宣传片重剪（文案 + 重排片段） | **P0** | 窗口 D | 2h |
| W4 | README 与提交材料对齐 v2 叙事 | P1 | 窗口 C（W2 之后） | 1h |
| W5 | SAFETY.md + 合规页 | P2 | 待定 | 1h |
| W6 | 三分钟演示动线 | 暂缓 | — | — |
| C1 | `/api/ask` 返回耗时与用量 | P1 | **Codex** | — |

**排序理由**：DM-02 和 W1 一起产出"这是真多智能体"的**唯一硬证据**（四个座位各调各的、system 互不含对方底牌，面板里能数）；W2/W3 是评委实际看到的两份东西，现在讲的还是旧故事，不换就等于前面全白做。W5 降级是因为**路演还早**，先把故事和证据立住。W6 等 W2/W3 定稿再写，否则要返工。

**DM-02 已由梦蝶局窗口基本完成**（0810 复核）：视野隔离、逐座位「真/脚」角标、Agent Card 战绩、案例卷宗进资讯库、卷宗架，全部已在 `public/app.html` 里跑通。剩余项见 `docs/WORK_ORDER_DM02_梦蝶局_A2A与案例卷宗.md` 末尾的复核记录，**不要当新活派出去**。

---

## W2 · 产品 PPT 重写 + 措辞清洗

**边界**：`public/os.js` 的 `PITCH` 数组（约 :1371）、`tools/pitch-data.json`、`tools/make-pptx.cjs` 的 `SLIDES`、以及 `rptGen` 附近的文案字符串。**不动渲染逻辑、不动图片资源引用方式、不动其他任何函数。**别的窗口在改 `os.js` 的别处，只做增量替换，不整段重写。

⚠️ **同一份文案在仓库里有三份独立拷贝**，全是手抄的，改一处会漏两处：

| 产物 | 文案源 | 谁看 |
|---|---|---|
| 应用内 PPT 窗口 | `public/os.js` 的 `PITCH`（图 `pitch/*.jpg`，多一个 `n` 讲稿字段） | 评委看 demo |
| 产品介绍 PDF | `tools/pitch-data.json` → `tools/make-pdf.cjs`（图 `docs/shots/*.png`） | **提交材料，评委带走的那份** |
| .pptx | `tools/make-pptx.cjs` 里硬编码的 `SLIDES` | 备用 |

**顺手把后两者收敛成一个源**：让 `make-pptx.cjs` 也读 `pitch-data.json`，删掉它自己那份硬编码。`os.js` 的 `PITCH` 因为要打进单机版单文件、且图片路径和字段不同，**保持手工同步**，但必须和 `pitch-data.json` 逐页对齐。

**目标 A：把 13 页从"功能巡礼"换成"一条机制"。**
现在的顺序是 封面/井/壳/键/水面/梦蝶/真言/集市/对表/分身/见证/倒影/梦醒——**梦蝶局排第 6 当游戏、分身排第 10 当功能，两者没接上；老板键排第 4，开场就把注意力带偏。**

新骨架（页数可增删，**顺序不能乱**）：

```
封面 → 井（痛点）→ 泡泡的一生（唯一机制，五段）
→ 吹出来 → 浮起来
→ 被验证·人（签名互保 / 薪资对表 / 机器人预热不计入）
→ 被验证·AI（梦蝶局 = AI 的信誉考场，四个座位视野隔离、各调各的）
→ 坑先摔一遍 + 案例卷宗（卷宗本身就是一颗泡泡）
→ 变成钱（¥99 机器人 / ¥599 真人 → 工资条·演示数据）
→ 沉底可捞
→ 四种载体一套规则（不许出现"平台"二字）
→ 老板键（场景准入证 + 一键述职是反向阀门 + 脉脉不能在工位上打开）
→ 量尺已经造好（诚实纪律 + AI 拿掉就塌）
→ 梦醒（子非鱼收尾）
```

**目标 B：措辞清洗。**全仓搜 `摸的鱼变干的活|包装|四层水路`：

- **一键述职的定性必须改**：从"AI 替你把摸的鱼包装成干的活"改成"你在这儿对过的表、接过的单、翻过的资料，一键写成能交差的段落"。**只改措辞，不动 `rptGen` 的数据源和生成逻辑**（数据源本来就读的是真实痕迹）
- 「四层水路」作为并列名词的表述，换成「一颗泡泡的一生」作为过程
- **产品整体的自嘲幽默是资产，保留**（「专业摸鱼版」「改了八版的假文档」这类不许清洗），只改一键述职这一处的定性

**配图**：`docs/shots/` 现有 13 张定帧图（D9 / E1 / P1-desktop / P2-bosskey / E2 / B2 / B3 / P4-skill / C1 / A3 / A4-漏斗 / P3-truefake / E3）**优先复用**——A4 漏斗正好可以承载「泡泡的一生」。新增页配不上就复用同一张，或用全局生图脚本以同题图垫图补。**不许为了迁就现有配图改叙事顺序。**

**验收**
1. PPT 从头放到尾，能答出 `NARRATIVE_v2` 第十节那三句话
2. **三份产物口径一致**：应用内 PPT、`node tools/make-pdf.cjs` 出的 PDF、`node tools/make-pptx.cjs` 出的 pptx，逐页比对无差异；`make-pptx.cjs` 已改为读 `pitch-data.json`，仓库里不再有第三份硬编码
3. 全仓 `摸的鱼变干的活` 零残留；PPT 里不出现"我们是一个平台"
4. 「人气 32 · 机器人预热 12（不计入）」至少出现一次
5. `node tools/build-dist.js` 通过 `$$` 自检
6. 旧 PDF 保留不覆盖，新的出 `docs/变大泡泡-产品介绍-v3.pdf`

**开场白（复制即发）**
```
在 D:\bianda-paopao 工作，按工单 docs\PLAN_0810_评委视角冲刺.md 的 W2 跑。
先完整读 docs\NARRATIVE_v2.md，那是叙事唯一事实源，slides 从它派生。
先注意一件事：同一份 slides 文案在仓库里有三份独立手抄的拷贝——
public\os.js 的 PITCH（应用内）、tools\pitch-data.json（→PDF，评委带走的那份）、
tools\make-pptx.cjs 里硬编码的 SLIDES。只改一份会漏两份，三份都要改，
并且顺手让 make-pptx.cjs 改成读 pitch-data.json，删掉它自己那份硬编码。
做两件事：一、把 13 页从「功能巡礼」换成「一颗泡泡的一生」这一条机制——
工单里有新骨架，页数可增删但顺序不能乱；重点是梦蝶局要从「第 6 页的游戏」
变成「AI 的信誉考场」和分身接上，老板键挪到收尾。
二、措辞清洗：一键述职的定性从「包装摸鱼」改成「沉淀收获」（只改文案不动 rptGen 逻辑），
「四层水路」这个并列名词换成「一颗泡泡的一生」这个过程。产品整体的自嘲幽默保留，别清洗。
os.js 只做增量替换别整段重写——另外两个窗口在并行改同一个文件的别处，动手前先 git status。
验收：三份产物逐页口径一致、PDF 出 v3 不覆盖旧的、全仓无「摸的鱼变干的活」、
不出现「我们是一个平台」、dist 过 $$ 自检。
读完先回我一句你怎么理解的，等我确认再开干。
```

---

## W3 · 宣传片重剪

**边界**：文案、字幕、剪辑产物在 `docs/` 下；成片需要同步到站内和单机版（见下）。**不改任何 `public/` 代码**，只替换视频文件。现成资产全部可复用，**不要重新生成视频素材**。

⚠️ **同一条片子在仓库里有三份拷贝**（md5 一致），只换一份等于没换：

| 路径 | 用途 |
|---|---|
| `docs/变大泡泡-45s-终版.mp4` | 提交材料 |
| `public/media/宣传片.mp4` | **站内桌面《新员工入职培训》放的就是它**——评委 demo 里看到的那个 |
| `dist/media/宣传片.mp4` | 单机版 |

新片定稿后三处都要更新（站内和 dist 保持文件名 `宣传片.mp4` 不变，`os.js:1493` 硬编码了这个路径，别改名）。`docs/` 下的旧终版保留不覆盖，出 `变大泡泡-45s-v2.mp4`。

**手上有什么**（先盘一遍再动手）：

- `docs/shots/video/` 14 条素材：`E1`(井) · `P1-desktop` · `P2-bosskey` · `P3 真真假假` · `P4 技能集市` · `A4 漏斗` · `B2梦蝶` · `B3 毒菜`×2 · `C1 三家作保` · `D0` · `D9 蝶与鱼` · `E2 梦门` · `E3 惊醒`
- `docs/rough/` 已排好的 c1–c14 + `list.txt` + `run-xfade.sh`（crossfade 链，直接改参数复用）
- `docs/vo/lines.txt` 现有 14 句旁白 + `01-14.wav` TTS + `tts.ps1`
- `docs/SUBTITLES-45s.srt` / `SUBTITLES.srt`
- 成品 `docs/变大泡泡-45s-终版.mp4`（**别覆盖，出新文件**）

**目标：换的是叙事顺序和三句关键词，不是画面。**旧版问题——老板键在第 4 句就出现（开场把注意力带偏）、梦蝶局只有"坑先摔一遍"一句（漏掉 AI 居民和案例卷宗）、"分身帮你接单"没说清凭什么值钱。

新节拍（14 拍，仍是 45 秒；片段沿用现有素材，**自己决定怎么对应**）：

```
1 痛点：一句真话没人听        8 坑先在梦蝶局摔一遍，不疼
2 所以造了台假 Windows        9 局里四个 AI 互相试探，谁都看不见谁的底牌
3 壳下面全是活人             10 摔过的坑存成卷宗，留给后面的人
4 说句真话，别人吹气就变大     11 你睡你的，分身在局里上班，攒的是它的简历
5 没人理就沉底，沉底可捞       12 你随手会的那点事真有人买：99 先答，接不住转真人
6 说真话要签名，吹牛绕道       13 老板来了？按一下——因为这一切发生在上班时间
7 工资别猜，三个同行帮你对     14 子非鱼，安知鱼之乐。变大泡泡
```

**红线**：不许编造用户数或成绩；工资条镜头若出现，「演示数据 · 未接入真实支付」必须可读；不出现"平台"二字。

**验收**
1. 新文案存 `docs/vo/lines-v2.txt`，TTS 重跑，字幕新出 `docs/SUBTITLES-45s-v2.srt`，时间轴对得上
2. 新片输出为 `docs/变大泡泡-45s-v2.mp4`，**旧终版保留不覆盖**
3. **`public/media/宣传片.mp4` 和 `dist/media/宣传片.mp4` 同步成新片**（文件名不变），起服务点桌面《新员工入职培训》放的是新版
4. 老板键那一下清晰可见且在**片尾**，不在开头
5. 看完能说出「它是社区不是工具 / AI 是居民 / 一句真话怎么变成钱」

**开场白（复制即发）**
```
在 D:\bianda-paopao 工作，按工单 docs\PLAN_0810_评委视角冲刺.md 的 W3 跑。
先完整读 docs\NARRATIVE_v2.md（叙事唯一事实源），再盘一遍 docs\shots\video\、docs\rough\、
docs\vo\ 里现有的素材——14 条片段、14 句旁白、TTS 脚本、xfade 链、字幕全是现成的，
这轮换的是叙事顺序和三句关键词，不要重新生成任何视频素材。
旧版三个问题：老板键第 4 句就出现把注意力带偏了；梦蝶局只有一句「坑先摔一遍」，
漏掉了「四个 AI 互相试探谁都看不见谁的底牌」和「摔过的坑存成卷宗留给后人」；
「分身帮你接单」没说清凭什么值钱。工单里有 14 拍的新节拍表，片段怎么对应你自己定。
注意：同一条片子仓库里有三份 md5 相同的拷贝——docs\变大泡泡-45s-终版.mp4（提交材料）、
public\media\宣传片.mp4（站内桌面《新员工入职培训》放的就是它，评委 demo 看到的）、
dist\media\宣传片.mp4（单机版）。只换一份等于没换，三处都要更新；
站内和 dist 的文件名必须还叫 宣传片.mp4，os.js:1493 硬编码了这个路径。
不改任何 public\ 代码，只替换视频文件。docs\ 下旧终版保留别覆盖，出 v2。
红线：不许编造用户数，工资条镜头的「演示数据·未接入真实支付」必须可读，不出现「平台」二字。
验收：lines-v2.txt + 新字幕时间轴对得上 + 三处视频都换成新片 + 老板键在片尾且清晰。
读完先回我一句你怎么理解的，等我确认再开干。
```

---

## W1 · AI 调用记录面板（已派，边界追加）

原目标不变：让评委当场看见 AI 真在被调用，降级标红写清原因，入口三秒可达。

**新增一条（因 DM-02 而来）**：面板要能承受**一局多次调用**的量——按会话/功能分组折叠，别铺成一条流水账。评委看的是「一局梦蝶局 · N 次调用 · 4 个独立 agent」这个汇总数字，展开才看明细。**这个数字是"我们是不是真多智能体"的直接答案。**

⚠️ **N 必须是面板真数出来的数，不许写死、不许凑整。**梦蝶局实际是开局四次并发 + 每轮质询一次现场，一局 4–7 次，视玩家出牌而定。

---

## W4 · README 与提交材料对齐（W2 之后做）

`README.md` 和 `docs/readme-bund/README.md` 现在都还写着「四层水路」。等 W2 定稿后同口径改：机制换成「一颗泡泡的一生」、加梦蝶局的双重定位（AI 信誉考场 + 坑先摔一遍）、加「机器人预热不计入」、一键述职正名。截图不用重拍。

---

## W5 · SAFETY.md（P2，路演前补）

匿名 + 八卦 + 薪资 + 伪装外壳四个雷区，三条防线（内容脱敏 / 三级情报+签名互保 / 薪资是聚合行情不是个人爆料），外加伪装外壳定位是隐私保护不是欺骗工具。**明说哪些没解决**——"这是我们打算长期投入的方向，不是已经攻克的难题"。全文不许出现"已解决/已杜绝/完全避免"。

---

## C1 · Codex

- `/api/ask` 返回耗时与用量（W1 面板已预留读取位）
- 回答 DM-02 的两个问题：`ai.gate` 限流阈值、同轮四并发是否会被拦、延迟能否接受
- 降级契约保持不变：无 key / 超额 / 超时 → `mode:'script'` + `why`

---

## 明确不做

- **不再优化视觉**——边际收益为零，过度精美反而加深"这是概念稿"的怀疑
- 不加新玩法、新窗口、新游戏（DM-02 的卷宗除外，它是叙事必需）
- 不动参赛仓；不碰 `server/`

---

## 派单顺序

| 时机 | 窗口 | 内容 | 冲突 |
|---|---|---|---|
| ~~立刻~~ | 梦蝶局窗口 | ~~DM-02~~ **已完成，不要重派** | — |
| 立刻 | 窗口 A | W1 | 只动 `os.js` 尾部追加 + `app.html` CSS 追加 |
| 立刻 | 窗口 D | W3 视频 | **零代码冲突，完全并行** |
| 立刻 | Codex | C1 | 只动 `server/` |
| 立刻 | 窗口 C | W2 | 动 `os.js` 的 `PITCH` 与文案 |
| W2 完成后 | 窗口 C | W4 | 只动两份 README |
| 路演前 | 任一 | W5、W6 | — |

**冲突提示**：DM-02 与 W1 都会碰 `public/app.html`，W1 与 W2 都会碰 `public/os.js`。三者**各自只在不同区域做增量**（DM-02 在 `#dreamGame`，W1 追加新模块，W2 只替换 `PITCH` 与文案字符串），可以并行，但**每个窗口下手前必须重读目标区域**，不许整段重写、不许拿旧行号。
